# Frontend (public browser variable)
# Leave empty to use same host + :4000 at runtime, or set explicit public API URL.
NEXT_PUBLIC_API_BASE=

# Backend
PORT=4000

# Optional deployment settings
NODE_ENV=production
